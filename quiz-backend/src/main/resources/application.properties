spring.application.name=quiz-backend

# MySQL (Workbench/local) connection
spring.datasource.url=jdbc:mysql://localhost:3306/online_quiz_system?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Djaadhi_09
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate defaults for local development
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Spring Security fixed credentials (for development only)
spring.security.user.name=admin
spring.security.user.password=admin123

# JWT Configuration
jwt.secret=mySecretKeyForJWTAuthenticationWithAtLeast256BitsOfEntropyAndSecurityForProductionUse
jwt.expiration=86400000
